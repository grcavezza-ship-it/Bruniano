name: Patch Studio Gallery Rows
on:
  push:
    paths:
      - .github/workflows/patch-studio-rows.yml
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: No-op
        run: echo cleanup